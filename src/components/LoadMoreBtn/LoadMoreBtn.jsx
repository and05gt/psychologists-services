import s from './LoadMoreBtn.module.css';

const LoadMoreBtn = () => {
  return (
    <button className={s.loadMoreBtn} type="button">
      Load more
    </button>
  );
};

export default LoadMoreBtn;
